/// Strucuture holding all the queues, drivers, service objects etc.
/// Somehow must interact with the EDS2Code generator to get a static memory definition
/// (number of SDO CLients etc.)
pub struct CanOpenStack;
